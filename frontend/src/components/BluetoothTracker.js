// Bluetooth Tracker — disabled for v1.0 Play Store launch.
// The @capacitor-community/bluetooth-le plugin (v8.x) requires Capacitor 6,
// but we're on Capacitor 5 for this release. Will re-enable in v1.1 once we
// upgrade Capacitor.

export default function BluetoothTracker() {
    return null;
}
